"use strict";  //treat all js code as a newer version

//alert(3+3)   we are using a nodeJS ,not a browser

console.log(3
            +
             3)  //code redability should be high at the time of we are writing the code avoid this write in in single line

let name="Yash";
let age=20;
let isLoggedIn=false;
let state;                                 //undefined

/*
   //number->range 2 to power to 53
   //bigint=>
   //string=>""
   //boolean=>true/false
   //null=>standalone value(type is=>object)
   //undefined=>
  //symbol=>unique
*/
  //object

  console.log(typeof(undefined))   //undefined
  console.log(typeof(null))        //object